 Frames-making
This application will gives frames from the videos 
3.	Write a program to convert the video into images.

•	Reads a video file using Python and OpenCV.
•	Extracts each frame from the video sequentially.
•	Provides flexibility to store frames in a folder for later use in AI tasks such as segmentation, classification, or training machine learning models.
•	Ensures that the program can handle different video formats (e.g., .mp4, .avi) and save images in standard formats (e.g., .jpg, .png).
